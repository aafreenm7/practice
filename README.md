# practice<br>
Notes of GIT HUB:<br>
ls: list files<br>
clear:clear terminal window<br>
clone: git clone link<br>
cd: change directory: go from outside folder to inside folder: cd folder_name<br>
-a : to see the hidden files which are accessible only through terminal: command: ls -a<br>
add: git add file name :add new or chnaged files in your working directory to the git staging area<br>
    :git add .<br>
commit: it is the record of the change<br>
     git commit -m "some message"<br>
push: upload local repo content to remote app<br>