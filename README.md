# practice<br>
Notes of GIT HUB:<br>
ls: list files<br>
clear:clear terminal window<br>
clone: git clone link
cd: change directory: go from outside folder to inside folder: cd folder_name
-a : to see the hidden files which are accessible only through terminal: command: ls -a
